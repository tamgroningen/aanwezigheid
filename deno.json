{
  "tasks": {
    "dev": "PORT=8791 deno run --allow-read --allow-net --allow-env publiek/server.js"
  },
  "deploy": {
    "org": "tam",
    "app": "aanwezigheid",
    "_let_op": "De nieuwe 'deno deploy' leest include/exclude hier NIET. Rol daarom altijd de map publiek/ uit: deno deploy --prod publiek"
  }
}

